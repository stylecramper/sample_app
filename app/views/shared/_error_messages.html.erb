<% if object.errors.any? # object generalizes this for any errors %>
	
	<div id="error-explanation">
		<h3><%= pluralize(object.errors.count, 'error') %> prohibited this <%= object.class.to_s.underscore.humanize.downcase %> from being saved:</h3>
		<p>There were problems with the following fields:</p>
		<ul>
		<% object.errors.full_messages.each do |message| %>
		<li><%= message %></li>
		<% end %>
		</ul>
	</div>
	
<% end %>